# 定期定額 App：自架設定說明

總共約 30 分鐘，只要做一次。做完之後：抓報價不花 Claude 額度，盤中 App 開著時每 20 秒更新一次，手機和電腦看到同一份資料。

## 檔案清單

| 檔案 | 用途 | 放哪裡 |
|---|---|---|
| index.html、sw.js、manifest.webmanifest、icon-*.png、apple-touch-icon.png | App 本體 | GitHub |
| config.js | 你的 Firebase 與轉接程式網址（第 1、2 步填） | GitHub |
| worker/worker.js | 報價轉接程式 | Cloudflare（第 2 步） |
| firestore.rules | 資料庫存取規則 | Firebase（第 1 步） |

---

## 第 1 步：Firebase（雲端資料庫與登入，約 10 分鐘）

1. 用 Google 帳號登入 https://console.firebase.google.com ，按「建立專案」，名稱隨意（例如 `dca-ledger`）。Google Analytics 可以關掉。
2. 左側「建構 → Authentication」→「開始使用」→ 登入方式選「電子郵件/密碼」→ 啟用第一個開關 → 儲存。
3. 左側「建構 → Firestore Database」→「建立資料庫」→ 位置選 `asia-east1（台灣）` →「以正式版模式啟動」。
4. 進入 Firestore 的「規則」分頁，把原本內容全部刪掉，貼上 `firestore.rules` 的內容 →「發布」。
5. 左上齒輪「專案設定」→「一般」→ 最下方「你的應用程式」按 `</>`（網頁）→ 暱稱隨意 → 註冊（不要勾 Firebase Hosting）。
6. 畫面會出現一段 `firebaseConfig`，把其中 `apiKey`、`authDomain`、`projectId`、`appId` 四個值，填進 `config.js` 對應的引號裡。

> 這四個值不是密碼，放在公開網頁上是 Firebase 的正常用法。資料安全靠第 4 步的規則：只有登入的你本人讀寫得到。

## 第 2 步：Cloudflare 報價轉接程式（約 10 分鐘）

1. 到 https://dash.cloudflare.com 免費註冊並登入。
2. 左側「Workers & Pages」→「建立」→「建立 Worker」→ 名稱填 `dca-quote` →「部署」。
3. 部署完按「編輯程式碼」，把編輯器裡的內容全部刪掉，貼上 `worker/worker.js` 的內容 → 右上「部署」。
4. 記下網址，長得像 `https://dca-quote.你的名稱.workers.dev`。
5. **測試（重要）**：用瀏覽器開 `https://dca-quote.你的名稱.workers.dev/quote?codes=0055,00685L`
   - 看到 `"ok":true` 和價格 → 成功，把網址填進 `config.js` 的 `workerUrl`（結尾不要加 `/`）。
   - 看到錯誤或 `"prices":{}` → 證交所可能擋了 Cloudflare 的連線，先把畫面截圖給 Claude，不要繼續往下做。

## 第 3 步：GitHub Pages（放網頁，約 5 分鐘）

1. 登入 GitHub → 右上「+」→「New repository」→ 名稱 `dca` → 選 **Public** → Create。
   - 免費帳號的 GitHub Pages 只能用 Public 倉庫。公開的只是程式碼，你的記帳資料在 Firebase，別人看不到。
2. 在倉庫頁面按「Add file → Upload files」，把 App 本體和 `config.js`（檔案清單第一、二列）全部拖進去 →「Commit changes」。`worker` 資料夾和 `firestore.rules` 不用上傳。
3. 倉庫「Settings → Pages」→ Source 選「Deploy from a branch」→ Branch 選 `main`、資料夾 `/ (root)` → Save。
4. 等 1～2 分鐘，網址是 `https://你的帳號.github.io/dca/`。

## 第 4 步：第一次使用與搬資料

1. 在 claude.ai 版的定期定額 App（或舊版紀錄簿）→「更多 → 下載完整備份」，拿到 `定期定額備份_日期.json`。
2. 用手機打開 `https://你的帳號.github.io/dca/` → 填 Email 和密碼 → 按「第一次使用：建立帳號」。
3. 「更多 → 從備份還原」→ 選那個 JSON 檔 →「合併還原」。
4. 加到主畫面：iPhone 用 Safari 的分享鍵 →「加入主畫面」；Android 用 Chrome 右上 ⋮ →「加到主畫面」。
5. 電腦用同一組 Email 登入，就會看到同一份資料。

## 第 5 步：上鎖（建議，約 3 分鐘）

1. **關閉別人註冊**：Firebase →「Authentication → 設定 → 使用者動作」→ 取消勾選「啟用建立（註冊）」→ 儲存。這樣只有你這個帳號能登入。
2. **轉接程式只給你的網頁用**：Cloudflare 編輯 `dca-quote` 的程式碼，把第 4 行 `const ALLOWED_ORIGIN = '*';` 改成 `const ALLOWED_ORIGIN = 'https://你的帳號.github.io';` → 部署。

---

## 平常怎麼運作

- **盤中**（週一到週五 9:00～13:30）：App 開著時每 20 秒更新現價和加權指數；收盤後抓到的就是收盤價。
- **月底價**：過了月份後，App 會自動向證交所補抓每檔標的當月最後一個交易日的收盤價，不用手動填。
- **大盤高點**：收盤指數超過你設的歷史高點時，自動更新高點。
- **離線**：沒網路也能打開看最後一次的資料；新增的紀錄會在連上網路後自動同步。

## 之後要改版

Claude 改好 `index.html` 後，到 GitHub 倉庫上傳覆蓋同名檔案即可。手機 App 下次打開（有網路時）就會拿到新版；若沒更新，完全關掉 App 再開一次。

## 疑難排解

| 狀況 | 處理 |
|---|---|
| 登入畫面顯示「還沒設定 Firebase」 | `config.js` 沒填好或沒上傳。確認四個值都在引號內 |
| 「更多 → 自動報價」顯示更新失敗 | 先用瀏覽器開第 2 步第 5 點的測試網址。測試網址正常但 App 失敗，通常是 `workerUrl` 打錯，或第 5 步的 `ALLOWED_ORIGIN` 網址寫錯（不要加 `/dca`） |
| 報價抓不到，但還是想更新損益 | 「更多 → 手動改現價」照舊可以用 |
| 忘記密碼 | 登入畫面填 Email，按「忘記密碼」收重設信 |

## 費用

Firebase、Cloudflare、GitHub Pages 都用免費方案。以一個人使用的量，遠低於各家的免費額度（Cloudflare 每天 10 萬次請求；盤中每 20 秒一次，一天約 800 次）。
